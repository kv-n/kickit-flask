# kickit-flask

TO START:

DO ONLY ONCE:
pip3 install virtualenv

TO RUN VIRTUAL .ENV
$ virtualenv .env -p python3
$ source .env/bin/activate

TO INSTALL DEPEND.:
pip3 install flask
$ pip3 freeze > requirements.txt

TO DEACTIVATE:
$ deactivate

TO RUN:
python app.py